# include <stdio.h>

int main(){
    float radius ; 
    printf("Enter the radius of the circle :- ");
    scanf("%f", &radius);
    printf("The Area of the circle is :- %f \n", 3.14 * radius * radius);
    return 0;
}